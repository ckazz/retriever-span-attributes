
== v1: as reported: OpenInference kind, with the query and documents on the gen_ai.* attributes 
  log stream:  retriever-attrs-v1-055ee269

  what was stored on the retriever span:
    type   :  retriever
    input  :  <empty>
    output :  []

  the stored span, as the read API returns it:
{
  "name": "query get_support_docs",
  "type": "retriever",
  "input": "",
  "output": []
}

    ok    nothing in the export request was refused
    ok    span is typed as a retriever
    ok    input is blank, as expected
    ok    output is blank, as expected

== v2: OpenInference kind, query and documents on input.value and output.value =
  log stream:  retriever-attrs-v2-055ee269

  what was stored on the retriever span:
    type   :  retriever
    input  :  How do I reset my password?
    output :  [{"content":"A password reset can be started from the account settings page","metadata":{}},{...

  the stored span, as the read API returns it:
{
  "name": "query get_support_docs",
  "type": "retriever",
  "input": "How do I reset my password?",
  "output": [
    {
      "content": "A password reset can be started from the account settings page",
      "metadata": {}
    },
    {
      "content": "The reset link expires after fifteen minutes and can be requested again",
      "metadata": {}
    }
  ]
}

    ok    nothing in the export request was refused
    ok    span is typed as a retriever
    ok    input carries the query
    ok    output carries the documents

== v3: OpenInference kind, output.value wrapping the list under a documents key 
  log stream:  retriever-attrs-v3-055ee269

  what was stored on the retriever span:
    type   :  retriever
    input  :  How do I reset my password?
    output :  [{"content":"A password reset can be started from the account settings page","metadata":{}},{...

  the stored span, as the read API returns it:
{
  "name": "query get_support_docs",
  "type": "retriever",
  "input": "How do I reset my password?",
  "output": [
    {
      "content": "A password reset can be started from the account settings page",
      "metadata": {}
    },
    {
      "content": "The reset link expires after fifteen minutes and can be requested again",
      "metadata": {}
    }
  ]
}

    ok    nothing in the export request was refused
    ok    span is typed as a retriever
    ok    input carries the query
    ok    output carries the documents

== v4: OpenInference kind, documents as a JSON string on retrieval.documents ===
  log stream:  retriever-attrs-v4-055ee269

  what was stored on the retriever span:
    type   :  retriever
    input  :  How do I reset my password?
    output :  []

  the stored span, as the read API returns it:
{
  "name": "query get_support_docs",
  "type": "retriever",
  "input": "How do I reset my password?",
  "output": []
}

    ok    nothing in the export request was refused
    ok    span is typed as a retriever
    ok    input carries the query
    ok    output is blank, as expected

== v5: OpenInference kind, documents as an array attribute of strings on retrieval.documents 
  log stream:  retriever-attrs-v5-055ee269

  the request was refused, and the HTTP status was a success anyway:
    spans rejected:  2 of the 2 sent
    reason given  :  Group 0: Validation error converting trace dict to Trace model: {"trace_id": "02a8cf3c-5840-4fc8-bc94-91a7e12b91b7", "trace_name": "support assistant turn", "field": ["retriever...

  spans the log stream holds after 20s:  <none>

    ok    the export request was refused
    ok    the refusal names the offending field
    ok    the retriever span did not land
    ok    the healthy parent span did not land either

== v6: no OpenInference kind, message lists on the gen_ai.* attributes, documents as the content 
  log stream:  retriever-attrs-v6-055ee269

  what was stored on the retriever span:
    type   :  retriever
    input  :  How do I reset my password?
    output :  [{"content":"A password reset can be started from the account settings page","metadata":{}},{...

  the stored span, as the read API returns it:
{
  "name": "query get_support_docs",
  "type": "retriever",
  "input": "How do I reset my password?",
  "output": [
    {
      "content": "A password reset can be started from the account settings page",
      "metadata": {}
    },
    {
      "content": "The reset link expires after fifteen minutes and can be requested again",
      "metadata": {}
    }
  ]
}

    ok    nothing in the export request was refused
    ok    span is typed as a retriever
    ok    input carries the query
    ok    output carries the documents

== v7: as v6 but typed explicitly with gen_ai.operation.name instead of db.operation 
  log stream:  retriever-attrs-v7-055ee269

  what was stored on the retriever span:
    type   :  retriever
    input  :  How do I reset my password?
    output :  [{"content":"A password reset can be started from the account settings page","metadata":{}},{...

  the stored span, as the read API returns it:
{
  "name": "query get_support_docs",
  "type": "retriever",
  "input": "How do I reset my password?",
  "output": [
    {
      "content": "A password reset can be started from the account settings page",
      "metadata": {}
    },
    {
      "content": "The reset link expires after fifteen minutes and can be requested again",
      "metadata": {}
    }
  ]
}

    ok    nothing in the export request was refused
    ok    span is typed as a retriever
    ok    input carries the query
    ok    output carries the documents

== v8: no OpenInference kind, but the query and documents sent the way v1 sends them 
  log stream:  retriever-attrs-v8-055ee269

  the request was refused, and the HTTP status was a success anyway:
    spans rejected:  2 of the 2 sent
    reason given  :  Group 0: Validation error converting trace dict to Trace model: {"trace_id": "b8515bce-ec08-4070-967f-73ec1db92524", "trace_name": "support assistant turn", "field": ["retriever...

  spans the log stream holds after 20s:  <none>

    ok    the export request was refused
    ok    the refusal names the offending field
    ok    the retriever span did not land
    ok    the healthy parent span did not land either

== v9: as v2 but each document carries its id inside metadata ==================
  log stream:  retriever-attrs-v9-055ee269

  what was stored on the retriever span:
    type   :  retriever
    input  :  How do I reset my password?
    output :  [{"content":"A password reset can be started from the account settings page","metadata":{"id"...

  the stored span, as the read API returns it:
{
  "name": "query get_support_docs",
  "type": "retriever",
  "input": "How do I reset my password?",
  "output": [
    {
      "content": "A password reset can be started from the account settings page",
      "metadata": {
        "id": "doc1"
      }
    },
    {
      "content": "The reset link expires after fifteen minutes and can be requested again",
      "metadata": {
        "id": "doc2"
      }
    }
  ]
}

    ok    nothing in the export request was refused
    ok    span is typed as a retriever
    ok    input carries the query
    ok    output carries the documents

== v10: as v6 but each document carries its id inside metadata =================
  log stream:  retriever-attrs-v10-055ee269

  what was stored on the retriever span:
    type   :  retriever
    input  :  How do I reset my password?
    output :  [{"content":"A password reset can be started from the account settings page","metadata":{"id"...

  the stored span, as the read API returns it:
{
  "name": "query get_support_docs",
  "type": "retriever",
  "input": "How do I reset my password?",
  "output": [
    {
      "content": "A password reset can be started from the account settings page",
      "metadata": {
        "id": "doc1"
      }
    },
    {
      "content": "The reset link expires after fifteen minutes and can be requested again",
      "metadata": {
        "id": "doc2"
      }
    }
  ]
}

    ok    nothing in the export request was refused
    ok    span is typed as a retriever
    ok    input carries the query
    ok    output carries the documents

== SUMMARY =====================================================================
  v1  PASS  nothing stored     as reported: OpenInference kind, with the query and documents on the gen_ai.* attributes
  v2  PASS  documents stored   OpenInference kind, query and documents on input.value and output.value
  v3  PASS  documents stored   OpenInference kind, output.value wrapping the list under a documents key
  v4  PASS  input only         OpenInference kind, documents as a JSON string on retrieval.documents
  v5  PASS  request rejected   OpenInference kind, documents as an array attribute of strings on retrieval.documents
  v6  PASS  documents stored   no OpenInference kind, message lists on the gen_ai.* attributes, documents as the content
  v7  PASS  documents stored   as v6 but typed explicitly with gen_ai.operation.name instead of db.operation
  v8  PASS  request rejected   no OpenInference kind, but the query and documents sent the way v1 sends them
  v9  PASS  documents stored   as v2 but each document carries its id inside metadata
  v10  PASS  documents stored   as v6 but each document carries its id inside metadata

  project retriever-span-attributes, log streams suffixed -055ee269
